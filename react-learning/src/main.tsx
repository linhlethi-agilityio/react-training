import React from 'react'
import ReactDOM from 'react-dom/client'
import ProviderManage from '@components/Count/Count'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <ProviderManage />
  </React.StrictMode>
)
